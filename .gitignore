node_modules/
.cache/
.npm/
dist/
# نواتج بناء Vite (تُعاد توليدها من client/ عند npm run build)
public/
*.zip
.arena/
.env
data/uploads/
data/backups/
data/*.sqlite*
# documents privés (P0) : factures, justificatifs, fichiers employés —
# ne doivent jamais atterrir dans l’historique git, même en résidu de tests.
data/private/
uploads/
.config/
.local/
*.traineddata
