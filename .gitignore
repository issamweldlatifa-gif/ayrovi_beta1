node_modules/
.cache/
.npm/
dist/
# نواتج بناء Vite (تُعاد توليدها من client/ عند npm run build)
public/
*.zip
.arena/
.env
data/uploads/
data/backups/
data/*.sqlite*
# documents privés (P0) : factures, justificatifs, fichiers employés —
# ne doivent jamais atterrir dans l’historique git, même en résidu de tests.
data/private/
uploads/
.config/
.local/
*.traineddata

# état des tests E2E locaux (regénéré à chaque exécution)
verify/.e2e-marker.json

# Capacitor/Android — sorties de build (le projet android/ lui-même est versionné)
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/

# APK livrables — jamais versionnés (régénérés par la CI : artifact AYROVI-debug.apk)
apk/
build-out/
