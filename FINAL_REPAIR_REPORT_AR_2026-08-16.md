# التقرير النهائي لإصلاح AYROVI

**التاريخ:** 16 أغسطس 2026  
**النسخة بعد الإصلاح:** `3.2.1`
**نقطة البداية:** `a2a9b023b1b131c475616c6f6db808b3d8ab9c53`  
**الحالة:** الإصلاحات البرمجية الحرجة ودفعة الاستكمال وإصلاح سجل الرجوع الشامل مكتملة، والنسخة `3.2.1` منشورة إنتاجيًا على Render.

> **الحالة الإنتاجية المؤكدة في 16 أغسطس 2026:** Render يشغّل `3.2.1` مع سجل تنقل داخلي شامل لزر Back. تسجيل Google وAI وVisual Search والصوت جاهزة. أضيف Facebook OAuth كاملًا لكنه يبقى معطّلًا آمنًا إلى أن ينشئ المالك تطبيق Meta ويضبط أسراره. البريد وOTP SMS ما زالا غير مفعّلين. Twilio ليس الخيار التشغيلي لتونس؛ الافتراضي أصبح Webhook لمزود تونسي. خيار CARD يبقى تدفق تحصيل يدوي آمن ثم تأكيد من Admin.

---

## 1) النتيجة النهائية

| التحقق | النتيجة |
|---|---|
| TypeScript للخادم والعميل | ✅ ناجح بلا أخطاء |
| اختبارات Vitest | ✅ **117/117** في 11 ملفًا |
| Build إنتاجي | ✅ ناجح |
| `npm audit` | ✅ 0 ثغرات معروفة |
| `git diff --check` | ✅ ناجح |
| Liveness `/api/health` | ✅ HTTP 200 — النسخة 3.2.1 |
| Readiness `/api/ready` | ✅ HTTP 200 وSQLite جاهزة |
| API غير موجودة | ✅ JSON 404 بدل HTML 200 |
| ضغط الأصول | ✅ Brotli متحقق حيًا |
| نسخة SQLite | ✅ أُنشئت وفُحصت بـ`quick_check=ok` |

المعاينة المحلية لا تحتوي أسرار الإنتاج. في Render أصبحت Google وAI وVisual Search والصوت مفعّلة فعليًا. Facebook مدمج لكنه غير مفعّل لغياب تطبيق Meta، ويبقى البريد وOTP SMS غير مفعّلين حتى توثيق نطاق البريد واختيار مزود SMS تونسي.

### فحص Render الفعلي — `https://ayrovi-beta1-1.onrender.com/`

دُمج Commit `586abb8` إلى `main` ونشر Render النسخة **3.2.0** بنجاح. أعيد تنفيذ Smoke test غير تخريبي، والنتائج:

- TLS 1.3 وشهادة صالحة وHSTS وHTTP/2.
- `/api/health` و`/api/ready` يعيدان 200؛ SQLite جاهزة.
- زمن الاستجابة الدافئ للواجهات المفحوصة يقارب 102–124ms.
- CSP الجديدة و`X-Request-ID` وJSON 404 تعمل في الإنتاج.
- تسريب `remark` أُغلق، والـLegacy Reels يعيد 410.
- Manifest وRobots وSitemap ووسوم SEO منشورة.
- إعدادات التجارة موجودة: 24 ولاية، عربون 20%، وCARD/FLOUCI/BANK_TRANSFER/POSTE.

الحالة الفعلية للتكاملات في هذه الخدمة:

- `/api/ready`: `assistant=true`, `visualSearch=true`, `voice=true`, `googleLogin=true`, `facebookLogin=false`, `sms=false`, `mail=false`.
- `/api/customer/auth/config`: `google.enabled=true`, `facebook.enabled=false`, و`phoneOtp.enabled=false`.
- `/api/customer/auth/google/start`: يعيد 302 إلى `accounts.google.com` مع callback صحيح إلى `https://ayrovi-beta1-1.onrender.com/api/customer/auth/google/callback`، وScopes هي `openid email profile`، مع وجود `state`.

بذلك تأكد استمرار Google OAuth بعد نشر 3.2.0: البداية تعيد 302 إلى Google مع callback الصحيح و`state`. صفحة الخصوصية `/privacy.html` وصفحة حذف البيانات `/data-deletion.html` تعيدان 200، ومحاولة حذف الحساب بلا جلسة تعيد 401، وFacebook غير المهيأ يعيد 503 بدل بدء تدفق ناقص. لم يُستكمل تسجيل دخول حقيقي أو طلب/بريد/SMS فعلي أثناء الفحص غير التخريبي حتى لا ننشئ بيانات إنتاج دون إذن.

### فحص إصلاح Back الشامل — النسخة `3.2.1`

نُشر Commit `82e5a12` على `main` وعلى فرع الإصلاح، ثم أكد `/api/health` انتقال Render فعليًا إلى **3.2.1**. السبب الجذري كان أن الواجهة العامة تغيّر React state فقط، بلا `history.pushState`، لذلك كان أول ضغط على Back يعيد المستخدم إلى موقعه السابق خارج AYROVI. عولج ذلك بطبقة موحدة تحفظ مسار طبقات AYROVI وعمقه داخل `window.history.state` وتستعيده عبر `popstate`.

يشمل الإصلاح المنشور:

- التنقل الرئيسي: القائمة، Product/Lens، المساعد، السلة، Checkout، الحساب، نجاح الطلب وAyvisi.
- صفحات CMS الأربع، Stories، Reels، التعليقات والـoverlays الاجتماعية.
- أقسام الحساب، تفاصيل الطلب، محرر العناوين، تسجيل/ربط الهاتف ومراحل OTP.
- مراحل Product Drawer وAYROVIX Lens، سجل Lens ومعلومات الكاميرا.
- قوائم المساعد، المرفقات، التعليقات/التقييم واختيار المنتج.
- الحفاظ على سجل AYROVI عند تنظيف OAuth query أو تغيير أقسام Admin.
- ضغط خطوات المصادقة المؤقتة بعد نجاح تسجيل الهاتف كي يصبح Back من Checkout عائدًا مباشرة إلى مصدره (السلة أو الصفحة السابقة).
- استبدال Checkout بنجاح الطلب بدل إبقاء نموذج مُرسل قابلًا لإعادة التفعيل عبر Back؛ النتيجة المطلوبة أصبحت `نجاح الطلب → Back → السلة/المصدر`، بينما `Checkout → Back → السلة/المصدر` قبل الإرسال.
- عدم السماح لزر Back بمغادرة AYROVI قبل استنفاد جميع إدخالات الموقع والوصول إلى الصفحة الرئيسية الفعلية.

أضيفت 6 اختبارات jsdom لسجل المتصفح: الجذر، Cart/Checkout/Success، الطبقات الاجتماعية، حفظ payload، توافق OAuth/Admin، ضغط مراحل المصادقة، و`goHome`. النتيجة الكاملة بعد الإصلاح: TypeScript ناجح، **117/117** اختبارًا ناجحًا، Build إنتاجي ناجح، `npm audit` بلا ثغرات، و`git diff --check` ناجح. أكد الفحص الحي أن Bundle الإنتاج يحتوي `__ayroviNavigationV1` وأن `/api/ready` حافظ على `assistant=true`, `visualSearch=true`, `voice=true`, `googleLogin=true`، مع بقاء Facebook/SMS/mail غير مفعلة للأسباب التشغيلية المذكورة أدناه.

---

## 2) الإصلاحات المنفذة

### أ) حماية Social / Reels

- نُقل Social rate limiter إلى ما قبل Public Router؛ أصبح فعالًا فعلًا.
- أضيف اختبار Regression يثبت ظهور HTTP 429 بعد بلوغ الحد.
- أُغلقت مسارات Reels القديمة غير المحمية بـHTTP 410.
- أصبح العميل يستخدم `/api/public/social/interact` لكل Like/View.
- Like وComment يتطلبان حسابًا وجلسة CSRF صحيحة.
- View وShare للضيف يتطلبان Session ID صالحة.
- التحقق من أن الهدف Story/Publication/Reel منشور فعليًا قبل حفظ التفاعل.
- منع إنشاء تفاعلات لأهداف وهمية، ما يمنع تضخم SQLite.
- Deduplication للمشاهدات والمشاركات حسب الحساب/جلسة الضيف.
- إضافة Unique partial indexes داخل SQLite لضمان عدم تضخيم المقاييس حتى مع طلبات متزامنة.
- توحيد عدادات Reels مع جدول التفاعلات الموثوق.
- تحويل `/social/counts` من حتى 60 Query منفصلة إلى Query مجمعة واحدة.
- إضافة Session ID تلقائيًا إلى طلبات Social في العميل.
- إصلاح تبديل Like في Reels ومنع العد المزدوج في الواجهة.

### ب) منع تسريب بيانات الإدارة

- `/api/public/social/publications` لم يعد يستخدم `SELECT *`.
- حقل `remark` والملاحظات التحريرية وحقول Admin لم تعد تظهر في Public API.
- Public Reels يعيد قائمة حقول عامة صريحة فقط.
- المحتوى المجدول لا يظهر قبل `publish_at`.

### ج) إصلاح قنوات المحتوى الاجتماعي

- Public API أصبح يعيد `id` و`slug` للناشرين.
- العميل يطابق Publication/Reel مع `channel_id` الصحيح.
- Stories تستمر في المطابقة حسب `slug`؛ لم يعد كل المحتوى يسقط على قناة AYROVI الرسمية بالخطأ.

### د) CSP وHTTP/API

- دمج سياستي CSP المتعارضتين في سياسة واحدة قبل كل Routes.
- إضافة `media-src 'self' blob: https:` كي تعمل فيديوهات CMS المسموح بها.
- إضافة `object-src 'none'`.
- إزالة Middleware ميتة كانت موجودة بعد SPA fallback.
- إضافة `X-Request-ID` لكل استجابة، مع قبول Request ID خارجي بعد التحقق من شكله.
- أخطاء JSON غير الصالح والحجم الزائد أصبحت JSON منظمة بدل صفحة Express HTML.
- API غير المعروفة أصبحت HTTP 404 بكود `API_NOT_FOUND` بدل إعادة صفحة SPA بحالة 200.

### هـ) استقرار عملية Node

- `uncaughtException` و`unhandledRejection` لم يعودا يتركان عملية قد تكون فاسدة حية.
- الخادم يوقف قبول الاتصالات، يغلق SQLite، ثم يخرج ليعيد Render تشغيله.
- الحفاظ على Graceful shutdown لـSIGTERM وSIGINT.

### و) حماية رفع Media من Admin

- صور JPEG/PNG/WebP تُفك وتُفحص وتُعاد معالجتها بالخادم قبل الحفظ.
- GIF والفيديوهات تخضع لفحص Magic signature بدل الثقة في Data URL فقط.
- خفض الحد الواقعي للفيديو إلى 10MB ليتوافق مع حد JSON بعد Base64.
- ملف يدعي أنه JPEG ومحتواه نص يُرفض بـHTTP 415 و`INVALID_MEDIA`.
- أضيف اختبار آلي يغطي الملف المزيف.

### ز) Checkout والبيانات

- توحيد Type طرق الدفع إلى `PaymentMethodCode` بدل Cast قديم يحصرها في COD/D17/FLOUCI.
- فلترة إعدادات طرق الدفع ضد Allowlist الخادم قبل استخدامها.
- توحيد الطرق الافتراضية مع الواجهة: CARD/FLOUCI/BANK_TRANSFER/POSTE.
- التحقق من رقم توصيل تونسي صحيح من 8 أرقام، مع قبول صيغ `+216` و`00216`.
- توثيق أن تحقق SMS للحساب اختياري، بينما رقم التوصيل إلزامي وصحيح.
- إزالة الوعد المضلل بأن البطاقة تُؤكد فورًا: الواجهة توضح الآن أن التأكيد والفاتورة والتتبع تأتي بعد تأكيد التحصيل.
- إضافة تحذير بعدم إرسال بيانات البطاقة في الرسائل.

### ح) أداء قاعدة البيانات

- إضافة `normalized_phone` وفهرس له.
- Backfill تلقائي للعملاء الموجودين عند بدء التطبيق.
- استبدال مسح جميع العملاء في JavaScript عند كل Checkout ببحث SQL مفهرس.
- الحفاظ على توافق البيانات القديمة وصيغ أرقام الهاتف الشائعة.
- إزالة كتل Seeds مكررة لـPublications وReels.

### ط) أداء Lens

- نقل ZXing إلى Dynamic import لا يُحمّل إلا عند دخول المستخدم إلى وضع QR/Barcode.
- حجم حزمة فتح Lens انخفض من **121.90KB gzip** إلى **12.02KB gzip**، أي انخفاض يقارب **90%**.
- حزمة QR البالغة **109.94KB gzip** أصبحت منفصلة ولا تُحمّل في مسار الصورة/الرابط العادي.

### ي) SEO وقابلية التثبيت

- إضافة Canonical وOpen Graph وTwitter cards وTheme color.
- إضافة `robots.txt` و`sitemap.xml`.
- إضافة `manifest.webmanifest` وApple touch icon.
- الملفات تدخل Build تلقائيًا عبر `client/public`.

### ك) التشغيل والنسخ الاحتياطي

- إضافة `/api/ready` الذي يفحص SQLite ويعرض حالة القدرات الخارجية دون كشف الأسرار.
- Render healthcheck أصبح يستخدم `/api/ready`.
- إضافة `npm run backup` لإنشاء نسخة SQLite متسقة.
- كل نسخة تُفحص بـ`PRAGMA quick_check`.
- حذف النسخ المحلية الأقدم من 14 يومًا افتراضيًا.
- إضافة رفع S3-compatible موقّع مباشرة إلى AWS S3 أو Cloudflare R2 أو Backblaze B2.
- إضافة Scheduler داخل عملية Render عبر `BACKUP_INTERVAL_HOURS` للوصول إلى القرص نفسه.
- إبقاء فشل النسخة الخارجية إلزاميًا اختياريًا عبر `BACKUP_REQUIRE_EXTERNAL=true` بعد نجاح أول اختبار.

### ل) بنية OTP SMS وTwilio Verify الاختياري

- إضافة Provider مباشر باسم `twilio_verify` دون إضافة SDK أو كشف أسرار للمتصفح.
- Twilio يرسل الرمز ويفحصه عبر Verify API؛ AYROVI لا يحتاج معرفة الرمز الذي أنشأه Twilio.
- تخزين Provider مع كل Challenge للحفاظ على توافق `console` وWebhook القديمين.
- إبقاء حدود الهاتف/IP وخمس محاولات ومدة Challenge المحلية فوق حماية Twilio.
- معالجة 429، فشل الإرسال، فشل التحقق، انتهاء الرمز، والرمز المستخدم مرتين برسائل API واضحة.
- إضافة `TWILIO_ACCOUNT_SID` و`TWILIO_AUTH_TOKEN` و`TWILIO_VERIFY_SERVICE_SID` إلى Render و`.env.example`.
- تحديث `/api/ready` ليعرض حالة SMS الحقيقية عبر Provider.
- إضافة 4 اختبارات Twilio جديدة للإعداد والإرسال والتحقق والرمز الخاطئ.
- OTP بقي لتسجيل الدخول وربط الطلبات القديمة فقط؛ رقم التوصيل الصحيح يكفي للـCheckout كما طلب المالك.

### م) نظافة واتساق

- رفع النسخة أولًا إلى `3.1.0` ثم `3.2.0`، وأخيرًا إلى `3.2.1` لإصلاح سجل الرجوع الشامل، في Package وHealth.
- تحديث README ودليل Render لسياسة الهاتف وطرق الدفع وGoogle/Facebook والنسخ الاحتياطي.
- إضافة `rel="noopener noreferrer"` للروابط التي تفتح تبويبًا جديدًا.
- ارتفع الإجمالي من 95 إلى **117 اختبارًا** عبر 11 ملفًا، منها 6 اختبارات متخصصة لسجل المتصفح.

### ن) Facebook OAuth الآمن

- إضافة Start/Callback server-side وفق Graph API `v26.0` بصلاحيتي `public_profile,email` فقط.
- ربط `state` بالمتصفح وProvider ومدة 10 دقائق عبر HttpOnly/SameSite Cookie.
- تبادل Code من الخادم وفحص Access Token عبر `debug_token` والتأكد من `app_id` و`user_id`.
- عدم تخزين Access Token بعد جلب الملف العام.
- عدم دمج الحسابات تلقائيًا ببريد Facebook غير الموثوق؛ الهوية المستقرة أو الربط الصريح فقط.
- Migration آمنة لتوسيع `customer_auth_identities` إلى `FACEBOOK` مع اختبار يحفظ هويات Google القديمة.
- زر Facebook يظهر فقط عندما تصبح `facebook.enabled=true`.
- إضافة فحص `facebookLogin` إلى `/api/ready`.

### س) الخصوصية وحذف الحساب

- نشر `/privacy.html` و`/data-deletion.html` وربطهما من Footer.
- إضافة حذف ذاتي للحساب محمي بالجلسة وCSRF وتأكيد صريح.
- حذف الملف الشخصي والهويات OAuth والجلسات والعناوين والمفضلة والبيانات المرتبطة.
- فصل الطلبات والفواتير القائمة عن الحساب بدل حذف السجلات التشغيلية/المحاسبية.

### ع) البريد وSMS

- أصبح فحص البريد يرفض Provider غير مدعوم أو `MAIL_FROM` ناقصًا/غير صالح بدل إعلان جاهزية زائفة.
- تغير افتراضي Render من Twilio إلى `CUSTOMER_OTP_PROVIDER=webhook` لمزود تونسي، مع إبقاء Twilio اختياريًا فقط.

---

## 3) ما ينقصنا قبل الإطلاق الإنتاجي

### حالة التكاملات

#### 1. متغيرات الخدمات الخارجية — ✅ Google وAI فعّالة؛ ⚠️ Facebook والبريد وSMS متبقية

فحص خدمة `ayrovi-beta1-1` بعد نشر 3.2.1 يثبت أن Google وAI وVisual Search والصوت فعالة. Facebook مدمج ويظهر `facebookLogin=false` حتى إنشاء تطبيق Meta وضبط `FACEBOOK_*`. يبقى `mail=false` حتى شراء نطاق وتوثيقه وضبط `MAIL_*`، و`sms=false` حتى اختيار مزود تونسي وربط API/Webhook.

#### 2. CARD — ✅ تدفق يدوي مقصود

أكد المالك أن CARD يعمل كتدفق تحصيل يدوي آمن ثم مراجعة/تأكيد من Admin. لا تُعد بوابة الدفع Online شرطًا ما دام هذا هو نموذج التشغيل المعتمد، والنص الحالي يوضح أن الفاتورة والتتبع يأتيان بعد تأكيد التحصيل ولا يطلب إرسال بيانات البطاقة في الرسائل.

يمكن إضافة بوابة Online لاحقًا كتطوير اختياري، مع Webhook موقّع وIdempotency، وليست عائقًا حاليًا أمام Pilot اليدوي.

### P0 المتبقي لاعتماد إطلاق عام واسع

#### 3. إكمال Environment Variables في خدمة Render

تأكدت صلاحية إعدادات Google وAI وVisual Search والصوت في خدمة `ayrovi-beta1-1`. يجب الحفاظ على `PUBLIC_BASE_URL` وGoogle callback للرابط الجديد. المتبقي هو:

- إنشاء تطبيق Meta ثم ضبط `FACEBOOK_APP_ID`, `FACEBOOK_APP_SECRET`, `FACEBOOK_CALLBACK_URL`.
- ضبط `MAIL_PROVIDER`, `MAIL_API_KEY`, `MAIL_FROM` بعد شراء نطاق وتوثيقه.
- ضبط Webhook مزود OTP التونسي بعد اختياره.
- ضبط `BACKUP_S3_*` وتغيير `BACKUP_REQUIRE_EXTERNAL=true` بعد نجاح أول رفع.

لا تُرسل أي أسرار في المحادثة أو GitHub.

#### 4. تفعيل مزود OTP تونسي بدل Twilio

Twilio غير معتمد تشغيليًا لأن المالك أوضح أنه لا يلائم احتياجه في تونس. يجب طلب تجربة وعرض من Orange Tunisie Messaging Pro وTunisieSMS، والتحقق من وصول OTP إلى Orange وOoredoo وTunisie Telecom ومن دعم Sender ID وتقارير التسليم. بعد اختيار المزود يمكن استخدام `CUSTOMER_OTP_PROVIDER=webhook` أو إضافة Adapter مباشر في `src/customer/otp.ts`. بعدها يجب أن تصبح `phoneOtp.enabled=true` وتُختبر رسالة حقيقية إلى رقم يحدده المالك خارج المحادثة العامة.

#### 5. الصفحات القانونية — ⚠️ الأساس التقني منشور والمراجعة القانونية متبقية

نُشرت سياسة خصوصية تقنية وصفحة حذف البيانات، وربطتا بالـFooter، كما أصبح حذف الحساب ذاتيًا من الملف الشخصي. المتبقي الذي يحتاج بيانات المالك ومراجعة قانونية فعلية:

- الشروط العامة للبيع/الوساطة وسياسة الإلغاء والإرجاع والاسترجاع.
- الاسم القانوني للشركة، المعرف الجبائي/السجل عند الاقتضاء، العنوان ووسائل الاتصال.
- تشغيل `contact@ayrovi.tn` فعليًا.
- تثبيت مدد الاحتفاظ القانونية بالمرفقات وإثباتات الدفع والفواتير والمحادثات.
- سياسة Cookies إذا أضيفت أدوات تحليل أو تسويق.

لا يصح اختلاق هذه البيانات برمجيًا؛ الصفحتان المنشورتان تحتاجان اعتمادًا قانونيًا قبل الإطلاق العام.

#### 6. Backup خارجي آلي — ⚠️ الكود والجدولة جاهزان، الاعتماد الخارجي متبقٍ

أصبح السكربت يدعم S3/R2/B2 بتوقيع server-side، وأضيف Scheduler يومي قابل للضبط داخل خدمة Render. نجح اختبار النسخة المحلية وفحص `quick_check=ok`. المتبقي تشغيليًا:

- إنشاء Bucket خارجي وضبط `BACKUP_S3_*`.
- نجاح أول Upload ثم تفعيل `BACKUP_REQUIRE_EXTERNAL=true`.
- وضع Lifecycle/Retention خارجي.
- اختبار Restore شهري موثق.
- ربط فشل النسخ بتنبيه خارجي.

#### 7. تحقق مستقل من رحلة الإنتاج

أكد المالك أن دورة الطلب والعربون والبريد والفاتورة كاملة. بعد تفعيل البريد ومزود OTP التونسي، يجب تنفيذ رحلة Smoke حقيقية على رابط Render الجديد وتوثيق النتيجة:

```text
تسجيل Google وFacebook وOTP
→ صورة Lens ورابط وQR
→ تأكيد سعر متجر
→ سلة
→ Checkout
→ دفع/عربون
→ رفع إثبات
→ مراجعة Admin
→ إرسال بريد
→ فاتورة
→ تتبع
```

---

## 4) ما ينقصنا بعد الإطلاق المحدود

### P1 — خلال أول أسبوعين

1. **E2E Browser:** Playwright على Chrome/Safari Mobile لمسارات الكاميرا وPermissions وDrawers وCheckout وAdmin.
2. **Observability:** Sentry أو بديل، OpenTelemetry، Dashboard للـ5xx والـlatency وتكاليف AI.
3. **Alerts:** مساحة القرص، فشل DB، فشل SMS/Mail، فشل Lens، نسبة 429 وارتفاع التكلفة.
4. **اختبار اختراق خارجي** قبل حملة تسويقية كبيرة.
5. **سياسة حذف Media:** تنظيف الملفات غير المرتبطة والمنتهية وإثباتات الدفع حسب Retention.
6. **قياس جودة Lens:** نسبة النتائج المفيدة، السعر المؤكد، زمن p95، الإلغاء والتحويل إلى طلب.

### P2 — عند نمو الاستخدام

1. Redis-backed rate limiting بدل الذاكرة.
2. Queue دائمة لطلبات AI والبريد والعمليات المكلفة.
3. PostgreSQL عند تشغيل أكثر من Instance أو زيادة حجم الطلبات.
4. Object storage/CDN للصور والفيديو والفواتير بدل قرص محلي واحد.
5. العربية وRTL عبر طبقة i18n منظمة.
6. مراجعة Accessibility فعلية بلوحة مفاتيح وقارئ شاشة.
7. صفحات عامة قابلة للفهرسة لكل محتوى/منتج بدل الاعتماد الكامل على SPA.

---

## 5) قرار الجاهزية

### جاهز الآن

- Demo وعرض المستثمر/الفريق.
- اختبار داخلي.
- Pilot محدود مع مراقبة بشرية وطرق دفع يدوية واضحة.

### المتبقي لإطلاق عام واسع موثق بالكامل

- شراء نطاق، توثيقه لدى مزود البريد، وضبط `MAIL_*` في خدمة Render الحالية.
- اختيار مزود OTP تونسي وربطه واختبار SMS حقيقي على الشبكات الثلاث.
- استكمال CGV وسياسات الإلغاء والهوية القانونية ومراجعة صفحتي الخصوصية والحذف المنشورتين.
- ربط النسخ الجاهز بـBucket خارجي وتنفيذ اختبار Restore.
- Smoke test مستقل على رابط Render للدورة الكاملة.
- E2E على أجهزة ومتصفحات حقيقية.
- Monitoring وتنبيهات إنتاجية.

**التقييم الحالي:** النسخة 3.2.1 منشورة وتنجح في فحوص البنية والأمان، وGoogle وAI وVisual Search والصوت فعالة. Facebook والبريد وSMS أصبحت بنيتها البرمجية جاهزة لكنها تحتاج حسابات/أسرار المزوّدين. الجاهزية التقديرية نحو **8.8/10**؛ المتبقي الحاسم تشغيلي وقانوني: Meta Live، نطاق وبريد، مزود OTP تونسي، Bucket/Restore، CGV وهوية قانونية، ثم Smoke وE2E ومراقبة إنتاجية.

---

## 6) أوامر التسليم

```bash
npm ci --include=dev
npm run typecheck
npm test
npm run build
npm audit
npm run backup
npm start
```

المعاينة الحية الحالية تعمل على المنفذ `3000`، وملف التقرير هو:

```text
FINAL_REPAIR_REPORT_AR_2026-08-16.md
```
