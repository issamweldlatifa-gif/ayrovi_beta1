# AYROVI — بناء نظام البوتيك v2 (خطة التنفيذ الكاملة)
**بدأ:** 25/09/2026 · **القاعدة:** ما فماش ترقيع، ما فماش تخمين، ما فماش بناء فوق القديم.

## المبدأ المعماري
طبقة عرض **جديدة كاملة** في `client/src/shop/`، ما تعتمد على **حتّى ملف** من الشاشات القديمة:
- الهوية الرسمية وحدها (`design/editorial` → `identity.json` / `glyphs.json` / الخطوط)؛ rayon 0، أكشن أسود، **صفر برتقالي**.
- CSS مستقلّ (`shop.css`) بسابقة `s-` وحدها — ما يلمس ولا يتلمس من الـ11 ملف CSS القديمة.
- عقد بيانات وحيد `ProductView`: السيرفر يبنيه مرّة، والشاشات تعرضو كيما هو. المكوّن **ما يحسبش ثمن، ما يخمّنش مقاس، ما يخترعش نصّ**. كل `null` يتعرض كـ**غياب**، موش كقيمة افتراضية.

## المراحل
| # | المرحلة | الحالة |
|---|---|---|
| 1 | عقد البيانات `types.ts` (ProductView, SizeOption, PriceView, الحالات الثلاثة) | ✅ |
| 2 | `shop.css` — جميع الجداول من الهوية الرسمية | ✅ |
| 3 | `SizeDrape` — درج المقاسات: 3 حالات، إسكالتين، يطلع 240ms | ✅ |
| 4 | `ProductPage` — صورة كاملة أول، ورقة تطلع وتطفّي، رايل عائم، شريط شراء ثابت | ✅ |
| 5 | `ProductGrid` — البطاقة + الوصف بنفس سطر الثمن المشطوب | ✅ |
| 6 | `LoadingView` — «نتثبّتو» موش «فشل» | ✅ |
| 7 | `BagPage` — السلة | ✅ |
| 8 | `AddressPage` — 3 أنماط توصيل | ✅ |
| 9 | `PaymentPage` — الوسائل التونسية، القرار من المصدر الوحيد | ✅ |
| 10 | `adapter.ts` — تحويل بيانات الإنتاج إلى `ProductView` في بلاصة وحدة | ✅ |
| 11 | اختبارات العقد والسلوك | ✅ |
| 12 | تركيب v2 مكان الشاشات القديمة + **فسخ القديم** | التالي |

## قاعدة الفسخ
الشاشة القديمة تتفسخ **كي** تولّي ما عندها حتّى مستورد، واختباراتها تتهاجر للجديد. الفسخ من غير هجرة الاختبار = ضياع ضمانة، وهذا ممنوع.
