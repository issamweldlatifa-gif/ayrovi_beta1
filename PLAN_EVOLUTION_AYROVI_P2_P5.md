# خطة تطوّر AYROVI — نظام بمستوى شركات عالمية + ثيم متطوّر

**المرحلة الحالية:** P2.0 مُغلقة ودُفعت (`origin/main = 980725e`, 471 اختبارًا أخضر). هذه الوثيقة هي **خطة** P2.1 ← P5، لا كود بعد. كل مرحلة تُنفَّذ بنفس العقد الذي جعل P0→P2.0 تمرّ بدون انكسار واحد.

---

## 0) الخلاصة والتوجّه

التوجّه الذي أقترحه: **نظام مونوليت موديولار (monolithe modulaire)** بطبقات مفروضة، لا « تطبيقات منفصلة » ولا « إعادة كتابة ». الفرق بين نظامنا ونظام شركة عالمية ليس عدد الشاشات — هو **انضباط الطبقات**: كل موديول يملك أنماطه وتحققه ومساراته وصلاحياته وتدقيقه، وكل شيء آخر مشترك (الصدَفة، الجدول الموحّد، البحث، التدقيق، الإشعارات، الثيم). عندنا اليوم الطبقة المشتركة بدأت تُبنى في P2.0؛ الخطة تُكملها وتُنشئ الموديولات فوقها.

ثلاثة مبادئ حاكمة:
1. **الإضافة قبل التعديل** — جدول/مسار/مكوّن جديد، ثم تحويل الاستهلاك، ثم (فقط عندProof) تقاعد القديم.
2. **قائمة واحدة للحقيقة** — سجلّ الوحدات `src/erp-core/modules.ts` + أوصاف `src/back-office/resources.ts` يولّدان التنقّل والصلاحيات والبحث والتدقيق والثيم. لا نسخة ثانية في الواجهة.
3. **الاختبار قفلٌ سلوكي** — أي مرحلة لا تُقلّل عدد الاختبارات (471 اليوم)، وكل باب من الأبواب الأربعة يجب أن يبقى صفرًا.

---

## 1) أين نحن اليوم (أرقام مأخوذة من الكود في هذا المستودع)

| المقياس | القيمة الحالية | من أين |
|---|---|---|
| وحدات سجلّ ERP | 21 — منها **7 active**، **10 legacy**، **4 planned** (`inventory`, `purchasing`, `accounting`, `automation`) | `src/erp-core/modules.ts` |
| جداول DDL موجودة | 87 (لا شيء باسم `inventory_*` / `suppliers` / `accounting_*`) | grep على `CREATE TABLE` |
| موارد معرَّفة في الـ framework | 39 (9 مشتقّة من المحرّك + 30 شاشة موجودة) | `src/back-office/resources.ts` |
| اختبارات | **471** على 45 ملفًا | `npm test` |
| CSS | 3 056 سطرًا على 12 ملفًا (`index.css` 1246، `admin.css` 696، `tokens.css` 160) | `wc -l` |
| ألوان مكتوبة حرفيًا في الواجهة | **392 موضعًا** وبرتقاليّان متنافسان (`#FF7A00` ×81 مقابل `#fe7003` ×37) وخمسة « أسود » مختلف (`#111111`, `#111318`, `#17151f`, `#111217`, `#17181c`) | grep |
| نظام تصميم | `tokens.css` موجود ومحكم الصياغة، لكن `client/src/design/` فيه **مكوّنان فقط** (`Button`, `AppHeader`) يستوردهما 8 ملفات | `ls` + grep |
| Dark mode | **غير موجود إطلاقًا** (`prefers-color-scheme` / `data-theme` = 0 نتيجة) | grep |
| RTL | جزئي: `dir="rtl"` على شاشات المجلّة + قواعد `[dir="rtl"]` في `journey.css` | grep |
| ثيم الموقع العام | **مُدارة أصلًا** عبر Admin → « واجهتي »: `interface-runtime.css` ينشر متغيّرات `--ayrovi-*` و5 إشكال (Violet/Pétrole/Sable/Océan/Monochrome) | `client/src/styles/interface-runtime.css` |

استنتاجان يُحدّدان شكل الخطة:
- الثيم الحديث **لا يحتاج محركًا جديدًا** — يحتاج توسيع `interface-runtime.css` + `tokens.css` وفرضهما. اسم إشكال « AYROVI Violet » بقِي بعد أن صار_brand_ برتقاليًا: هذا نوع الانحراف الذي يُصلَح بتوحيد الرموز.
- الموديولات « planned » الأربع **لا جداول لها بعد**: بناؤها إضافة كاملة (لا هدم)، وهذا أفضل وضع لعدم الكسر.

---

## 2) عقيدة «بدون كسر» — القواعد السبعة

1. **DB: إضافة فقط.** لا `DROP`، لا `TRUNCATE`، لا تغيير نوع عمود، لا إعادة تسمية جماعية. عمود جديد = `NULL`-able أو بقيمة افتراضية؛ جدول جديد = `CREATE TABLE IF NOT EXISTS` idempotent؛ التوثيق: كل موديول يبدأ بـ migration + اختبار يثبت idempotence (ثلاث تشغيلات متتالية).
2. **لا إعادة تسمية لمسار API.** مسار جديد يُضاف، القديم يبقى ويُعلَّم `canonicalOf`/`deprecated` في الوصف. كل 270 مسارًا حاليًا تبقى مستجيبة.
3. **محرّك واحد لكل مفهوم.** جدول بيانات/نموذج/بحث/تنبيهات/صلاحيات/تدقيق: **واحد**. أي مرحلة تُضيف « نسخة ثانية للمناسبة » مرفوضة ضمنيًا — تُعمَّم النسخة الموجودة (كما فُعل مع `DataTable` في P2.0: props اختيارية، 23 نداءً قائمًا لم يتغيّر ماركبها).
4. **الصلاحيات: لا نظام ثانٍ.** `erp_role_permissions` + `can()` + أرضية legacy. توسيع السجلّ (21 مدخلًا مجمّدة باختبار) **قرار صريح منك**، لا تفصيل تقني.
5. **التدقيق: `writeAuditEvent()` هو القلم الوحيد.** لا `INSERT` مباشر في أي مرحلة.
6. **بوابة الخروج لكل مرحلة:** `tsc` خادم 0 · `tsc -p tsconfig.client.json` 0 · `npm run build` ok · `npm test` ≥ العدد السابق بلا أي انكسار · **worktree نظيف** (شجرة مستقلة) بنفس النتيجة · ثم commit + push.
7. **لو تعارض اختبار قائم مع متطلب جديد → توقّف + تقرير**، لا « تعديل الاختبار ليسير ». (هذا هو المسار الذي أنقذ P2.1 وP2.0.)

---

## 3) البنية المستهدفة (الطبقات)

`explorations/architecture-couches.svg` — من الأسفل إلى الأعلى:

1. **البيانات**: `migrations/` (إضافة فقط) + جداول دومينية.
2. **النطاق** `src/<domain>/<module>/`: `types.ts` · `validation.ts` · `<entities>.ts` · `routes.ts` · `permissions.ts` · `audit.ts` · `bootstrap.ts` — مع معاملة (handler) **ناعمة**: الـ SQL والتدقيق داخل الخدمات.
3. **السجلّ**: `modules.ts` (الوحدات وحالتها) + `back-office/resources.ts` (أوصاف الموارد) — مصدرًا واحدًا للتنقّل والقدرات والبحث.
4. **الوصول**: `permissions.ts::can()` على `erp_role_permissions` + أرضية legacy؛ تسمية `module:action:resource:scope`.
5. **التدقيق والأحداث**: `audit.ts` (قلم وحيد) + `erp_events` كناقل + `erp_notification_deliveries` كـ outbox.
6. **واجهة مشتركة**: `components.tsx` (المحرّك) + `client/src/design/` (العناصر الأولية) + `back-office/` (الصدَفة والموارد).
7. **الثيم**: `tokens.css` (القيم) → `interface-runtime.css` (النشر الوقتي) → الشاشات. لا لون حرفيًا بعد P3.

---

## 4) ترتيب بناء الموديولات (P2.1 ← P4)

الترتيب مقصود: **أولًا ما يُصلِح duplication قائم، ثم سلسلة القيمة (مخزون→مشتريات)، ثم المال، ثم الأتمتة** — لأن كل مرحلة تُوسّع الطبقة المشتركة التي تحتاجها التالية.

| # | المرحلة | ما يُبنى | جداول جديدة (إضافة فقط) | بوابة الخروج | الحجم |
|---|---|---|---|---|---|
| **P2.1** | كتالوج داخل الصدَفة + توحيد | شاشات `catalogue-*` تصير الموردَ المرجعي؛ `products`/`brands` القديمة تُعلَّم `canonicalOf` وتبقى؛ **حذف 8 جداول مرسومة يدويًا** (`AiLabPages`, `SocialAdminPage` ×2, `HeroVisualsPage`, `TrustBarPage`, `StoriesStudio`) لفائدة `DataTable` | لا شيء | نفس 471 اختبارًا + اختبارات توحيد؛ `ResourceWorkspace` يستهلكه شاشةٌ حقيقية واحدة على الأقل | M |
| **P2.2** | **Inventory** (`inventory` → active) | مستودعات، أرصدة، حركات (entrée/sortie/ajustement)،جرد، حدّ أدنى وتنبيه؛ **الكميات هنا، لا في `products`** | `inventory_stock_items`, `inventory_movements`, `inventory_stocktakes`, `inventory_stocktake_lines` | `GET/POST /api/inventory/*` + مورد في الـ framework + صلاحيات `inventory:read/write/adjust/approve` + تدقيق + بحث شامل + Deep link؛ **لا يلمس `crm_warehouse_dispatches`** (جسر إضافة فقط) | L |
| **P2.3** | **Purchasing** (`purchasing` → active) | موردون، أوامر شراء، استلام جزئي، سعر购入 بالعملة، ربط الاستلام بالـ arrival-ingestion **بإضافة حقول فقط** (لا إعادة كتابة) | `suppliers`, `purchase_orders`, `purchase_order_lines`, `goods_receipts`, `goods_receipt_lines` | دورة كاملة: طلب → موافقة → استلام → حركة مخزون (P2.2) — كل انتقال مكتوب عبر `writeAuditEvent()` | L |
| **P3** | **الثيم المتطوّر** (§5) | لا شيء | لا شيء (فقط CSS/رموز) | 0 لون حرفي خارج `tokens.css`؛ dark + light + RTL + كثافة مُختبَرة بصريًا | M |
| **P4.1** | **Shipping** توحيد | `deliveries` و`crm_shipment_*` موجودة ومتشابهة: توحيد تحت مورد واحد بدل اثنين، مع إبقاء المسارين | لا شيء | تكافؤ API مُثبَت باختبار مقارنة (نفس الطلب ⇒ نفس البنية) | M |
| **P4.2** | **Accounting** (`accounting` → active) | دليل حسابات، قيود يومية مزدوجة، ربط الفاتورة/الدفع (`invoices`, `payments`) بمصدر القيد، ميزان مراجعة | `accounting_accounts`, `accounting_journal_entries`, `accounting_journal_lines` | توازن القيد مفروض في `validation.ts` ومختبَر؛ لا تعديل على `invoices` | L |
| **P5** | **Automation** (`automation` → active) | قواعد على ناقل `erp_events`، تنفيذ idempotent، إعادة محاولة، سجلّ تشغيل | `automation_rules`, `automation_runs`, `automation_run_steps` | إعادة استخدام `assistant_tool_idempotency` كنمط؛ الإشعارات تُستهلك من outbox ERP بدل قناة ثالثة | L |

**لماذا هذا الترتيب بالضبط:** P2.1 يُنظّف ما بنيناه للتو (تكلفة منخفضة، ربح مرتفع: لا مصدرَي حقيقة)؛ P2.2/P2.3 يبنيان العمود الفقري التشغيلي الذي تطلبه كل بقية الأنظمة (لا مشتريات بلا مخزون، لا محاسبة بلا مشتريات)؛ الثيم في P3 لأنّه بعد أن تستقرّ الشاشات — وإلّا أُعيد تزيين شاشاتٍ ستتغيّر؛ المحاسبة والأتمتة في الآخر لأنّهما أكبر انكشاف على البيانات التاريخية.

---

## 5) الثيم المتطوّر — خمس خطوات، كلها قابلة للتراجع

| خطوة | ما يحدث | ضمان عدم الكسر |
|---|---|---|
| **T1 — توحيد الرموز** | كل لون/ظل/نصف قطر/سرعة حركة يصير متغيّرًا في `tokens.css`؛ برتقالي واحد (`#fe7003`)؛ عائلة « أسود » واحدة بدرجاتها؛ حذف `--ayrovi-purple` من `journey.css` لصالح أسماء دلالية | اختبار حارس: `grep` يمنع `#[0-9a-f]{3,6}` خارج `tokens.css` (نفس نمط اختبارات المصدر المستخدمة في المستودع) — يبدأ من الصفر المتبقّي بعد كل مرحلة |
| **T2 — عناصر أولية حقيقية** | `client/src/design/` من 2 مكوّنات إلى ~18 (Button, Field, Select, Modal, Table cell, Tabs, Toast, Empty state, Badge, Skeleton, Tooltip, Switch, Avatar…)، و**المحرّك الحالي يعيد التصدير منها** بدل أن يُنسخ | `components.tsx` يبقى الواجهة العامة: الاستيرادات القائمة لا تتغيّر (إعادة تصدير)، فلا يُجبر أي ملف على تعديل |
| **T3 — الوضع الليلي + الكثافة + RTL** | `data-theme="light|dark|auto"` على `<html>` + مفتّح في صدَفة P2.0 (بجانب الكثافة الموجودة)؛ RTL **عالمي** بدل `dir` اليدوي، بمنطق `inset-inline`/`margin-inline` (موجود جزئيًا فعلًا) | الوضع الفاتح = القيمة الافتراضية الحالية حرفيًا؛ اختبار ثابت: بدون مفتاح، لا يتغيّر بكسل واحد في الماركب المصطَلَ |
| **T4 — طبقة الموقع العام** | توسيع **نفس** `interface-runtime.css`: أشكال جديدة (Midnight, Sand Pro…), نصف قطر، ارتفاع، كثافة، لون ثانوي، وخطّ — كلُّها تُنشر من « واجهتي » الموجودة، مع الحفاظ على مفاتيح `pricing_config`/`settings(category=INTERFACE)` | إشكال الافتراضي = ما يراه العميل اليوم؛ إضافة مفاتيح فقط، لا تغيير اسم مفتاح. اختبارات `interface-config.test.ts` (قائمة) تبقى خضراء |
| **T5 — حركة وإيحاءات** | انتقالات 120–180 ms، `prefers-reduced-motion` محترم، حالات فارغة مُرسومة بـ SVG داخلي، هياكل تحميل (skeletons) بدل « Chargement… » | لا مكتبة حركة جديدة؛ لا network fonts؛ لا شيء في `index.html` |

مِقياس النجاح البصري ليس رأيًا: **392 → 0** لونًا حرفيًا، **12 → 3** ملفات CSS للنظام (tokens + runtime + طبقة تطبيق)، **2 → 18** عنصرًا أوليًا مُعتمَدًا، وdark/light/RTL/kثافة كلها تعمل على نفس الماركب.

---

## 6) ما لن أفعله (حدود مُعلَنة)

- لا إعادة كتابة لـ `ArrivalIngestionPage`/`arrival-ingestion.css` (ملفات مجمّدة بقرار سابق)، ولا لـ CRM/CMS/Finance الموجود — **إضافة** وربط بالصدَفة فقط.
- لا « big bang »: لا شاشة تُهجَر قبل أن تعمل الجديدة بنفس الـ deep link ونفس الصلاحيات ونفس التدقيق.
- لا جدول « إعدادات مستعمِل » جديد قبل أن نثبت أن `localStorage` لا يكفي (تأجيل P2.0 موثّق ولا يزال قائمًا).
- لا وحدة جديدة قبل إغلاق السابقة ببواباتها.

## 7) المخاطر والنقاط المجهولة

| معرّف | المسألة | الأثر على الخطة |
|---|---|---|
| `UNKNOWN-012` | نطاق الموارد البشرية/الأجور غير محدّد | يؤثّر على صلاحيات حقول P2.2 (من يرى الأرصدة/التكاليف) — أحتاجه قبل P2.3 |
| `UNKNOWN-002` | حارستان قديمتان معطوبتان | تُصلَحان بإضافة صريحة، لا بتغيير سلوك مفتوح |
| `UNKNOWN-006` | ملكية `crm_stores` | يسدّ طريق P4.1 (الشحن مرتبط بالمتاجر) |
| S-01 | توحيد `products` ↔ `catalogue` يمسّ 3 قراءات قديمة | لا حذف قبل اختبار يثبت عدم وجود قارئ؛ غير ذلك يبقى `canonicalOf` |
| S-02 | الثيم الداكن يفضح كل لون حرفي منسي | لهذا T1 (توحيد الرموز) قبل T3 (الوضع الليلي)، لا العكس |

## 8) قرارات أحتاجها منك (قبل كتابة أول سطر من P2.1)

1. **اتجاه الثيم**: ثلاث تجارب مُرفَقة (Console فاتح كثيف / Midnight داكن / Soft RTL) — أيّها الأساس؟ (توصيتي: Console نهارًا + Midnight كخيار، على نفس الرموز).
2. **نطاق الثيم**: الإدارة فقط أولًا، أم الإدارة + الموقع العام معًا في P3؟
3. **ترتيب الموديولات**: موافقة على P2.1 → Inventory → Purchasing → **ثم** الثيم (P3)؟ أم الثيم قبل المخزون؟
4. **`UNKNOWN-012`**: هل الموارد البشرية/الأجور داخل نطاق P2–P4 أم خارجها؟

الوثائق المرئية المصاحبة (مجلد `explorations/`، لا تُستورَد من التطبيق): `roadmap-p2-p5.svg` · `architecture-couches.svg` · `THEME_LAB.html` (تجربة حيّة: فاتح/داكن/RTL/كثافة على عناصر حقيقية) · `theme-direction-A-console.png` / `-B-midnight.png` / `-C-soft.png` · `theme-evolution-p2-p3.png`. وثلاث صور اتجاهات + لوحة تطوّر واحدة في `explorations/`.
