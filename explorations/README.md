# استكشافات P3 — الثيم والاتجاه البصري

ملفات **قرار بصري**، لا كود تشغيلي: لا شيء في التطبيق يستوردها (ولا `THEME_LAB.html` نفسه). تُقرأ قبل التنفيذ لا بعده.

| ملف | ما هو | كيف تُثِق به |
|---|---|---|
| `THEME_LAB.html` | مختبر حيّ: نفس الماركب تحت 3 اتجاهات × فاتح/داكن × كثافة × RTL/LTR × «بلا حركة». الرموز المكتوبة فيه هي المقترَحة لـ `client/src/design/tokens.css`. | **هذا هو الدليل القوي**: HTML/ CSS حقيقيان، ما تراه هو ما سيطبَّق. افتحه في المتصفح وجرّب المفاتيح. |
| `theme-direction-A-console.png` | اتجاه **Console**: فاتح، حواف حادّة، كثيف، ظل شبه معدوم (Stripe/Linear). | صورة مفهومية مولّدة — تُقرأ لاتجاه الوزن البصري **لا** للنصوص (قد تكون غير مفهومة). |
| `theme-direction-B-midnight.png` | اتجاه **Midnight**: داكن، غرف تحكّم، ظل عميق، برتقالي محصور بالحالة النشطة. | نفس التحفّظ. |
| `theme-direction-C-soft.png` | اتجاه **Soft**: أركان واسعة، شريط جانبي بلمسة دافئة، بطاقة تفاصيل جانبية. | نفس التحفّظ. |
| `theme-evolution-p2-p3.png` | لوحة تطوّر من 4 إطارات: الإدارة القديمة → صدَفة P2.0 → نظام P3 → منتج ناضج مع drawer ووضع ليلي. | تصوّر تطوّر، لا لقطات فعلية. |
| `roadmap-p2-p5.svg` | خطّ زمني للمراحل P2.1 ← P5 + بوابات الخروج. | SVG مكتوب يدويًا: النصوص حرفية. |
| `architecture-couches.svg` | الطبقات السبع المستهدفة ومصدر الحقيقة في كل طبقة. | SVG مكتوب يدويًا. |

**ما تغيّر فعلًا في الأرقام** (من `grep` على المستودع اليوم، لا من التخمين): 392 لونًا مكتوبًا حرفيًا في CSS/TSX، برتقاليّان متنافسان (`#FF7A00` ×81 مقابل `#fe7003` ×37)، خمسة « أسود » مختلفة، 12 ملف CSS بمجموع 3 056 سطرًا، `client/src/design/` فيه مكوّنان فقط يستوردهما 8 ملفات، **لا dark mode إطلاقًا**، RTL جزئي (`dir="rtl"` يدوي على شاشات المجلّة). الهدف بعد P3: الألوان الحرفية = 0، ملفات CSS للنظام = 3، عناصر أولية ≈ 18، وdark/light/RTL/كثافة على محرّك واحد.
